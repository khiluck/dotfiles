# venv весит ~770 МБ и пересоздаётся из requirements.txt через setup.sh
venv/
__pycache__/
*.pyc

# Арбуз собирается из скачанного OBJ (см. tools/make_watermelon.py).
# Сам файл не коммитим: лицензия исходной модели неизвестна, а репозиторий
# уезжает на GitHub. Пересобирается одной командой.
models/Watermelon.glb
